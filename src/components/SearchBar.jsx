import {View, StyleSheet, Text} from 'react-native';

const SearchBar = () => {
    return (<View>
        <Text>SearchBar</Text>
    </View>);
};

const styles = StyleSheet.create({});

export default SearchBar;